#ifndef CONST_HPP
#define CONST_HPP

const int WIDTH = 1920;//a size of a frame
const int HEIGHT = 1080;
const int MIN_PIX = 16;
const int FPS = 60;
const float FRAME_TIME = 1 / (float) FPS;
const float COMPUTATION_TIME = FRAME_TIME;
const double G = 6.67E-11;
const int VERTEXES = 20;
const double PI = acos(-1);

#endif
